name: Cleanup Docker Hub Tags

on:
  schedule:
    - cron: '30 13 * * *'

jobs:
  cleanup:
    runs-on: ubuntu-latest

    steps:
      - name: Delete old docker images on hub
        run: |
          echo "Deleting docker files"
          echo -e "\n\n::notice:: Deleted"